// copy the JSOn data to clipboard
// Ctrl+Shift+p => Shortcut for VS Code where we can search for anything, setting
// Paste as Json and privde the model name

export interface MovieCard {
    id: number;
    title: string;
    posterUrl: string;
}